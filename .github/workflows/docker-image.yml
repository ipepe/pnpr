name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby-version: ['3.1', '3.0', '2.7']
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image for ${{ matrix.ruby-version }}
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
